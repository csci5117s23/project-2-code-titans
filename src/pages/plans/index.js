import React, { useState } from "react";
import {useAuth, useUser } from "@clerk/nextjs";
import "bootstrap/dist/css/bootstrap.min.css";
import {
  Container,
  Nav,
  Navbar,
  Card,
  Row,
  Col,
  Button,
} from "react-bootstrap";
import Navigation from "@/components/Navigation";
import Head from "next/head";
import { useRouter } from "next/router";
import PlanCard from "@/components/PlanCard";

export default function PlansPage() {
    const [plans,setNewPlans] = useState(["yuh","nah","fam"]);


    const { isLoaded, userId, sessionId, getToken } = useAuth();
    const router = useRouter();

    if (!isLoaded) return <></>;
    else if (isLoaded && !userId) router.push("/");
    else {

        const planList = plans.map((plan) => (
            <PlanCard name={plan} expenditure={5} summaryData={"yeah and?"} activeStatus={false}></PlanCard>
        ));

        return (
            <>
                <Head>
                    <title>Current Plans</title>
                    <meta name="description" content="Generated by create next app" />
                    <meta name="viewport" content="width=device-width, initial-scale=1" />
                    <link rel="icon" href="/duckget-logo.png" />
                </Head>
                <Navigation/>
                <Container className="mx-auto">
                    {planList}
                    {/* <ul className="place-items-center items-center self-center">
                    </ul> */}
                </Container>
            </>
        )
    }
}